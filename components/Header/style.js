import {StyleSheet}from 'react-native'

const estilo = StyleSheet.create({

    titulo: {
        fontSize: 35,
        fontWeight: 'bold',
        marginTop: 20,
        color: 'white',
      },
      subtitulo: {
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 15,
        color: 'white',
      },
      tituloCard: {
        fontSize: 15,
        fontWeight: 'bold',
        color: 'white',
      },
      porcentagemCard: {
        fontSize: 15,
        fontWeight: 'bold',
        color: 'white',
      },


})

export default estilo;